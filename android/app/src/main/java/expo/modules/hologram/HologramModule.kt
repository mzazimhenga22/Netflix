package expo.modules.hologram

/**
 * HologramModule — stub kept only so the expo-modules-autolinking scanner
 * doesn't break.  The actual native view is registered via the old-bridge
 * HologramPackage + HologramViewManager (SimpleViewManager), which is
 * added manually in MainApplication.getPackages().
 *
 * We intentionally do NOT register a View() block here to avoid a naming
 * collision with HologramViewManager.getName() == "HologramModule".
 */

// This file is intentionally left minimal.
// All view registration is handled by HologramViewManager via HologramPackage.
